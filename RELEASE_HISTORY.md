# ClashBox 版本历史

## 2.0.0 · 2026-09-24 · BaiLe 维护分支

从上游 1.7.4 基线和工作区已有的 1.8.0 开发状态开始，进入自维护大版本。

- 修复 VPN 启动、停止、快速重启、前后台恢复和蜂窝网络切换时的连接稳定性问题。
- 修复消息停在“正在发送”、核心待重载、冷启动加载失败、节点无法切换等问题。
- 修复旧订阅失败状态复活造成的后台重试风暴，减少 Telegram 等长连接反复重连。
- 优化订阅导入、格式兼容、校验失败保护、配置数据库并发写入和自动更新调度。
- 优化节点测速的进度保持、页面切换、结果稳定性和后台切回后的 UI 状态。
- 优化 TUN、DNS、IPv6、MTU、socket 保护、TCP 长连接和安卓兼容容器适配。
- 修复配置编辑页面底部内容被遮挡等界面问题。

## 1.7.4 · 上游基线

本地 Git 历史中可追溯的上游版本基线。包含核心升级、本地网卡注入、配置编辑与 YAML 解析优化、大配置下载、访问控制在线搜索及相关体验修复。

接手时本地工作区曾使用 `1.8.0` 作为开发版本号，未找到与之对应的上游正式版本记录；因此应用内历史以可追溯的 `1.7.4` 作为原作者版本和自维护版本的分界。

后续每次发布都需要同时更新：

1. `AppScope/app.json5` 中的 `versionName` 和 `versionCode`；
2. `entry/src/main/ets/components/Start/UpdateLog.ets` 顶部历史条目；
3. 本文件顶部的版本记录；
4. release HAP 构建产物和真机回归记录。
