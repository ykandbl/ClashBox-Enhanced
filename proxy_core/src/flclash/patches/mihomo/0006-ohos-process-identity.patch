--- a/component/process/process_linux.go
+++ b/component/process/process_linux.go
@@ -64,28 +64,25 @@ type inetDiagResponse struct {
 
 func findProcessName(network string, ip netip.Addr, srcPort int) (uint32, string, error) {
 	uid, inode, err := resolveSocketByNetlink(network, ip, srcPort)
-	if runtime.GOOS == "android" {
-		// on Android (especially recent releases), netlink INET_DIAG can fail or return UID 0 / empty process info for some apps
-		// so trying fallback to resolve /proc/net/{tcp,tcp6,udp,udp6}
-		if err != nil {
-			uid, inode, err = resolveSocketByProcFS(network, ip, srcPort)
-		} else if uid == 0 {
-			pUID, pInode, pErr := resolveSocketByProcFS(network, ip, srcPort)
-			if pErr == nil && pUID != 0 {
-				uid, inode, err = pUID, pInode, nil
-			}
+	// Linux-based HarmonyOS TUN uses the same procfs socket tables as Android,
+	// but the netlink view can omit sockets while a packet is being handed from
+	// the compatibility container. Try procfs on every build when netlink fails
+	// or returns the unowned UID 0.
+	if err != nil || uid == 0 {
+		pUID, pInode, pErr := resolveSocketByProcFS(network, ip, srcPort)
+		if pErr == nil && (err != nil || (uid == 0 && pUID != 0)) {
+			uid, inode, err = pUID, pInode, nil
 		}
 	}
 	if err != nil {
 		return 0, "", err
 	}
 	pp, err := resolveProcessNameByProcSearch(inode, uid)
-	if runtime.GOOS == "android" {
-		// if inode-based /proc/<pid>/fd resolution fails but UID is known,
-		// fall back to resolving the process/package name by UID (typical on Android where all app processes share one UID).
-		if err != nil && uid != 0 {
-			pp, err = resolveProcessNameByUID(uid)
-		}
+	// HarmonyOS builds use the Linux network stack, but the TUN source address
+	// can be rewritten and the socket inode may disappear during lookup. A
+	// known UID still identifies the app, so use the UID fallback for Linux too.
+	if err != nil && uid != 0 {
+		pp, err = resolveProcessNameByUID(uid)
 	}
 	return uid, pp, err
 }
@@ -134,6 +131,8 @@ func resolveSocketByNetlink(network string, ip netip.Addr, srcPort int) (uid uint32, inode uint32, err error) {
 	}
 
 	err = ErrNotFound
+	var portUID, portInode uint32
+	portMatched := false
 	for _, msg := range messages {
 		if len(msg.Data) < inetDiagResponseSize {
 			continue
@@ -141,13 +140,14 @@ func resolveSocketByNetlink(network string, ip netip.Addr, srcPort int) (uid uint32, inode uint32, err error) {
 
 		response := (*inetDiagResponse)(unsafe.Pointer(&msg.Data[0]))
 
-		// always set to allow fallback when check fails
-		uid, inode, err = response.UID, response.INode, nil
-
 		// check src port
 		if binary.BigEndian.Uint16(response.SrcPort[:]) != uint16(srcPort) {
 			continue
 		}
+		if !portMatched {
+			portUID, portInode = response.UID, response.INode
+			portMatched = true
+		}
 
 		// check src IP
 		var src netip.Addr
@@ -168,10 +168,14 @@ func resolveSocketByNetlink(network string, ip netip.Addr, srcPort int) (uid uint32, inode uint32, err error) {
 		}
 
 		// this is the one we want
-		break
+		return response.UID, response.INode, nil
 	}
-
-	return
+	if portMatched {
+		// In a userspace TUN, source IP can be rewritten before lookup. The
+		// local source port remains stable and is the useful OHOS fallback.
+		return portUID, portInode, nil
+	}
+	return 0, 0, ErrNotFound
 }
 
 func resolveProcessNameByProcSearch(inode, uid uint32) (string, error) {
--- a/tunnel/tunnel.go
+++ b/tunnel/tunnel.go
@@ -354,16 +354,27 @@ func resolveMetadata(metadata *C.Metadata) (proxy C.Proxy, rule C.Rule, err erro
 				if !features.CMFA {
 					// normal check for process
 					uid, path, err := process.FindProcessName(metadata.NetWork.String(), metadata.SrcIP, int(metadata.SrcPort))
+					// Keep a UID even when the inode-to-process lookup races with a
+					// closing socket. This is useful to the Harmony bridge and to the
+					// connection inspector's UID fallback.
+					if uid != 0 {
+						metadata.Uid = uid
+					}
 					if err != nil {
-						log.Debugln("[Process] find process error for %s: %v", metadata.String(), err)
+						// On HarmonyOS compatibility containers the socket can live in
+						// another namespace, so the Linux /proc lookup may fail even
+						// though the platform bridge can still provide an identity. Do
+						// not skip the package resolver in that case.
+						log.Debugln("[Process] find process error for %s: %v; trying platform resolver", metadata.String(), err)
 					} else {
 						metadata.Process = filepath.Base(path)
 						metadata.ProcessPath = path
 						metadata.Uid = uid
-
-						if pkg, err := process.FindPackageName(metadata); err == nil { // for android (not CMFA) package names
-							metadata.Process = pkg
-						}
+					}
+					// For Android/Harmony package names, try the platform resolver
+					// both after a normal lookup and after a namespace miss.
+					if pkg, pkgErr := process.FindPackageName(metadata); pkgErr == nil && pkg != "" {
+						metadata.Process = pkg
 					}
 				} else {
 					// check package names
